functions to be implemented in Admin_student

+checkEvent(event_id); //mao ni katong murag sign-in/out unta
+getEventRecords(event_id); //returns all student records in a specific event
+getStudentRecord(stud_id); //returns all records of a particular student
+enrollStudent(stud_id); //returns void, updates isEnrolled attribute of student to 'true'
+addStudent(idnum, fname, minit, lname, password, username, year); 
+editStudent(stud_id, whatToEdit, newValue);